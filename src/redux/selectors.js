// Select the user name
export const selectUserName = (state) => state.auth.userDetails.name;

// Select the profile picture
export const selectProfilePic = (state) => state.auth.userDetails.profilePic